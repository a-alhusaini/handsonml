# Learning Nx

```elixir
Mix.install([
  {:scholar, github: "elixir-nx/scholar"},
  {:nx, "~> 0.5.0", override: true}
])
```

## Section

```elixir
x = for n <- 1..100, do: [n]
x = x |> Nx.tensor()
y = for n <- 1..100, do: n + 2
y = y |> Nx.tensor()

model = Scholar.Linear.LinearRegression.fit(x, y)
Scholar.Linear.LinearRegression.predict(model, Nx.tensor([12299]))
```
